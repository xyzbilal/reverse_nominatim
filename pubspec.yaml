name: reverse_nominatim
description: OSM Nominatim reverse geocoder.
version: 0.0.1
repository: https://github.com/illabo/reverse_nominatim

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  dio: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.7
  json_serializable: ^3.5.1

flutter:
